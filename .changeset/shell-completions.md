---
default: minor
---

#### Add `--completions <shell>` to generate bash, zsh, and fish completion scripts

`scratchsmith --completions bash|zsh|fish` prints a completion script to stdout for the
chosen shell.
