---
default: minor
---

Every release now ships a CycloneDX SBOM of Scratchsmith's own dependency graph (`scratchsmith-v<ver>.cdx.json`, generated by syft from `Cargo.lock`). It's listed in `checksums.txt`, so the existing cosign signature and SLSA provenance cover it too.
